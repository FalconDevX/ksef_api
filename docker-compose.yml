services:
  backend:
    build: .
    container_name: ksef-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - ksef-network

networks:
  ksef-network:
    external: true